import React from 'react';
import { View, Text, Input, AsyncStorage, StyleSheet } from 'react-native';

export default class SignInScreen extends React.Component {
  render() {
    return(
      <View>
        <Text>Sign In</Text>
      </View>
    );
  }
}
